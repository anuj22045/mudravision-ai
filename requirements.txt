# MudraVision AI — Python Dependencies
#
# Install all dependencies:
#   pip install -r requirements.txt
#
# Phase 1 & 2: CNN Training + Prediction Pipeline
tensorflow>=2.13.0        # Deep learning framework — CNN training & inference
numpy>=1.24.0             # Numerical computing — array operations, argmax
Pillow>=10.0.0            # Image loading fallback (used by some TF utilities)
matplotlib>=3.7.0         # Plotting training curves (used in notebooks)
scikit-learn>=1.3.0       # Confusion matrix, classification report, train/test split

# Phase 3: OCR + NLP  (install when starting Phase 3)
# pytesseract>=0.3.10     # Python wrapper for Tesseract OCR
# opencv-python>=4.8.0    # Image preprocessing before OCR (grayscale, threshold)
# nltk>=3.8.1             # NLP: tokenization, stopwords, lemmatization

# Phase 4: Streamlit App  (install when starting Phase 4)
# streamlit>=1.28.0       # Web UI framework for the coin recognition app
